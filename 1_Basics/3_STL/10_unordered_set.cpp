// unordered_set: unique but 'not' sorted, order is random
// time complexity is O(1), worst case: O(N)
// all functions work except lower_bound() and upper_bound()

#include<bits/stdc++.h>
int main(){
    unordered_set<int> us;

    return 0;
}